# EasyGenerator Environment Variables
# REQUIRED: Copy this file to .env and fill in all values
# cp .env.example .env

# ============================================
# EXTERNAL ACCESS CONFIGURATION
# (These variables are used to access services from your computer)
# ============================================

# MongoDB Port - Access MongoDB from your computer: localhost:27017
MONGODB_PORT=27017

# Backend Port - Access API from your computer: http://localhost:3000
BACKEND_PORT=3000

# Frontend Port - Access website from your computer: http://localhost
FRONTEND_PORT=80

# Frontend API URL - Used by frontend (browser) to call backend API
REACT_APP_API_URL=http://localhost:3000

# ============================================
# INTERNAL CONFIGURATION
# (These are required for containers to work, but not directly accessible from outside)
# ============================================

# MongoDB Credentials (required for MongoDB container initialization)
MONGO_INITDB_DATABASE=easygenerator
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-secure-password-here

# Backend Configuration (required for backend container)
JWT_SECRET=your-secret-key-change-in-production-must-be-strong
NODE_ENV=development
FRONTEND_URL=http://localhost
